// Cambia esto por la URL real donde tienen corriendo el servidor de AYLYES
// (el mismo que usan para entrar al panel en el navegador). Si es local para
// pruebas, deja el localhost.
export const API_BASE = "http://localhost:3000";
